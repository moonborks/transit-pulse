# Dev Environment Setup

The compose.yml in this directory creates containers for a dev environment, and some tools for interacting with said tools.
Before attempting to run the backend code, run the following command to setup a dev environment.

```
# call these commands in this directory

docker compose up -d # builds all containers
docker compose down # tear down containers
```
